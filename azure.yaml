# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: kbchat
services:  
  app:
    language: dotnet
    project: ./KBChat.AppHost/KBChat.AppHost.csproj
    host: containerapp
hooks:
 postprovision: 
      shell: pwsh
      run: ./customInfra/deploy-roleassignments.ps1 
      interactive: true
